# Frontend dev origin for CORS (Codespaces will give you a forwarded URL; paste it here if needed)
FRONTEND_ORIGIN=http://localhost:5173

# Backend port
PORT=8080

# Optional: tweak concurrency for fetching PDFs
FETCH_CONCURRENCY=3

# Optional: request timeout (ms)
FETCH_TIMEOUT_MS=25000
